<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.ekart.repository.CartRepository">

    <insert id="addToCart">
        INSERT INTO cart (user_id, product_id, quantity)
        VALUES (#{userId}, #{productId}, #{quantity})
    </insert>

    <select id="findByUserAndProduct" resultType="com.example.ekart.model.Cart">
        SELECT * FROM cart
        WHERE user_id = #{userId}
        AND product_id = #{productId}
    </select>

    <update id="updateQuantity">
        UPDATE cart
        SET quantity = #{quantity}
        WHERE user_id = #{userId}
        AND product_id = #{productId}
    </update>

    <select id="getCartWithProducts" resultType="com.example.ekart.model.CartResponse">
    SELECT 
        c.product_id AS productId,
        p.name AS name,
        p.price AS price,
        p.image AS image,
        c.quantity AS quantity
    FROM cart c
    JOIN products p ON c.product_id = p.id
    WHERE c.user_id = #{userId}
    </select>

    <delete id="removeFromCart">
        DELETE FROM cart
        WHERE user_id = #{userId}
        AND product_id = #{productId}
    </delete>

    <delete id="clearCart">
        DELETE FROM cart
        WHERE user_id = #{userId}
    </delete>

</mapper>